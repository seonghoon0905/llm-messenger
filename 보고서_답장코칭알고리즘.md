# 한국어 메신저 답장 코칭 알고리즘 기술 문서

---

## 1. 시스템 구성

| 구성 요소 | 역할 |
|---|---|
| Flutter 앱 | 채팅 UI, 타이핑 감지 타이머, 피드백 카드 렌더링 |
| FastAPI 서버 (`main.py`, 포트 8000) | 분석 API, GPT 호출, WebSocket 브로드캐스트 |
| 한국어 NLU 서버 (`korean_nlu/main.py`, 포트 8001) | KOTE 감정 분류, 3i4K 화행 분류 |
| OpenAI GPT (`gpt-4.1`) | 답장 재작성 및 피드백 생성 |
| SQLite (사용자별 로컬 DB) | 메시지 저장, 수정·삭제 상태 관리 |
| SharedPreferences | 자동 피드백 On/Off 설정 영속화 |

---

## 2. 자동 피드백 트리거

타이핑을 멈추고 **2초가 경과**하면 피드백 분석이 자동 실행된다.

```
텍스트 변경 감지 (_onControllerChanged)
  → 텍스트가 비거나 / 자동 피드백 Off → 타이머 취소 후 종료
  → 기존 타이머 취소 + 2초 타이머 재시작
        │
        └── 2초 경과 → _handleAiFeedback() 호출
```

**예외 처리**
- `_isFeedbackLoading = true`이면 중복 호출 방지
- 자동 응답 생성 직후: `didUpdateWidget`에서 `isAutoReplyLoading: true→false` 전환을 감지, 진행 중인 타이머를 즉시 취소하고 다음 텍스트 변경도 억제(`_suppressNextFeedback = true`)

---

## 3. 분석 파이프라인

### 3.1 전체 흐름

```
사용자 draft
    │
    ▼
POST /analyze-draft
    ├── NLU 서버 호출 (포트 8001)
    │     ├── /emotion-scores → KOTE 감정 점수
    │     └── /speech-act    → 3i4K 화행 분류
    │     (NLU 불가 시 키워드 매칭 fallback)
    │
    ├── registerMode 판단 (미설정 시 대화 맥락으로 GPT 추론)
    │
    ├── ThinGate 판단 (_is_clearly_safe_to_skip_llm)
    │     ├── 위험 신호 감지 → shouldInvokeLlm = true
    │     └── 안전 조건 충족 → shouldInvokeLlm = false
    │
    └── llmCandidatePayload 생성 후 반환

shouldInvokeLlm = true
    │
    ▼
POST /llm-assist
    ├── 시스템 프롬프트 (정적)
    ├── 유저 프롬프트 (동적)
    └── GPT 호출 → {shouldFeedback, feedback, rewrite, reason}
```

### 3.2 NLU 분석

**감정 점수 (`source: "kote"`)**

| 점수 | 의미 |
|---|---|
| `distressScore` | 고통·불안 |
| `angerScore` | 분노·짜증 |
| `burdenScore` | 부담·피로 |
| `warmthScore` | 따뜻함·감사 |
| `neutralScore` | 중립·확인 |

분석 대상: 상대방 마지막 메시지(`partnerEmotionScores`) + 사용자 draft(`myDraftEmotionScores`)

NLU 서버 미가용 시 키워드 사전으로 fallback (`source: "fallback"`).

**화행 분류 (`source: "3i4k"`)**
`question` / `request` / `statement` / `command` 등으로 분류.

---

## 4. ThinGate — LLM 호출 조건

`_is_clearly_safe_to_skip_llm()` 함수가 판단. 아래 신호 중 하나라도 감지되면 GPT 호출.

| 조건 | 판단 기준 |
|---|---|
| 상대방이 질문 | `partnerSpeechAct == "question"` 또는 질문 키워드 |
| 상대방이 요청·명령 | `speechAct in ["request", "command"]` 또는 요청 키워드 |
| 상대방이 제안·초대 | "같이", "하자", "어때" 등 |
| 상대방이 고통·불안 표현 | `distressScore` 관련 키워드 |
| 상대방이 불만·갈등 표현 | "서운해", "화났어", "너 때문에" 등 |
| draft에 강한 표현 | 짜증·공격적 표현 키워드, `angerScore ≥ 0.4` |
| draft에 무시·냉담 표현 | "알아서", "어쩌라고", "됐어" 등 |
| draft 부정 감정 합산 ≥ 0.4 | `distress + anger + burden ≥ 0.4` |
| 격식 불일치 | `registerMode == "formal"` 이고 `formalProb < 0.5` |
| 부정 감정 연속 | 상대방 최근 2개 이상 메시지에서 부정 감정 연속 |
| 감정 악화 예상 | `emotionShift.direction == "worsened"` |

**LLM 생략 조건 (모두 충족 시)**
- 위험 신호 없음
- NLU 서버 사용 가능 (`source: "kote"` / `"3i4k"`)
- 또는 단순 사회적 교환 (감사, 확인, 인사 등)

**ThinGate 7개 규칙**

| ID | 규칙 | 심각도 |
|---|---|---|
| `formal_mismatch` | 격식 모드인데 draft가 캐주얼 | medium |
| `question_not_answered` | 상대 질문에 답 부족 | high |
| `request_not_addressed` | 상대 요청에 반응 부족 | medium |
| `harsh_expression` | 강하거나 공격적 표현 감지 | high |
| `emotion_shift_worsened` | 답장이 감정을 악화시킬 것으로 예상 | high |
| `distress_dismissive` | 상대 고통 상황에서 draft가 무심함 | high |
| `negative_streak_cold_reply` | 부정 감정 연속 상황에서 짧거나 차가운 draft | medium |

---

## 5. GPT 프롬프트 구조

시스템 프롬프트(정적)와 유저 프롬프트(동적)로 구성된다.

### 5.1 시스템 프롬프트 (정적 — 항상 동일)

모든 호출에서 동일하게 전달되는 규칙 집합이다.

---

**말투 규칙 — 2단계 기준**

1단계와 2단계로 위계가 명확히 분리되어 있다.

- **1단계(절대)**: `[대화 모드]`가 존댓말이면 전체 존댓말, 반말이면 전체 반말. 예외 없음.
- **2단계(보정)**: `[나의 말투 예시]`로 어미·어휘 수준을 맞춤. 예시 어미가 `~어요/~할게요`면 rewrite도 동일 어미 사용. 예시보다 격식이 높거나 낮아지지 않도록 수준을 맞춘다.
- 2단계는 1단계 범위 안에서만 적용된다. 예시가 없으면 1단계만 따른다.

---

**대화 목적과 감정 표현 구분**

rewrite에서 살려야 할 것과 제거할 것을 구분한다.

살려야 할 것(대화 목적):
- 사용자가 전하려는 정보·요청·거절·제안·상황 설명
- 서운함 자체가 핵심 메시지인 경우 (본인의 느낌으로 표현)
- 반복 패턴이 관계에 미치는 영향에 대한 우려

제거 대상(감정 표현):
- 비난, 빈정거림, 단정적 거부, 항의, 상대 탓, 일방적 통보, 압박

---

**충동적 포기 vs 실제 결론 판단**

draft에 포기 표현이 있을 때 대화 전체 맥락으로 판단한다.

- 대화 내내 무언가를 얻거나 개선을 원했는데 마지막에만 포기 선언 → 감정 표현으로 보고 실제 목적을 살린다
- 대화 흐름상 종료·취소·거절이 명백한 의사결정으로 보일 때 → 핵심 결론으로 보고 그 방향을 유지한다

"결론 유지" 규칙은 실제 의사결정에만 적용한다. 감정에서 나온 충동적 포기는 의사결정이 아니다.

---

**상대방 행동 언급 기준**

① **지금 하는 행동에 대한 요구·불평** → rewrite에서 완전히 제거
draft에 "~하지 마세요", "왜 자꾸 ~해요", "그만 ~해요" 패턴이 있으면, 상대 행동 언급을 일절 하지 않는다. softened 표현도 금지. 사용자의 현재 상황·계획·답변만 전달한다.

② **반복 패턴이 관계에 미치는 영향에 대한 우려** → rewrite에서 유지
"항상", "맨날", "매번" 등으로 반복성을 지적하면서 협력·관계가 어려워진다는 우려를 표현한 경우. 단정적 비난은 제거하고 "이런 상황이 반복되면 함께 하기 어려울 것 같다"는 형태로 표현한다.

**구분 기준**: "지금 당장 멈추라는 요구인가" vs "이 패턴이 관계에 어떤 영향을 주는지에 대한 우려인가"

---

**rewrite 작성 규칙 (9가지)**

1. 말투는 2단계 기준을 따른다. 공문서·관료적 문체는 피한다.
2. 핵심 결론·입장은 절대 뒤집지 않는다. 톤·표현만 바꾸고 결론은 유지한다.
3. 단정적·통보형 톤은 제안형·문의형으로 부드럽게 풀어준다.
4. 짧은 도입부(상황 인정 또는 본인 상황 설명) + 핵심 목적 구조. 한두 문장으로 충분.
5. 상대가 서운함·지적을 표현한 직후라면 그 원인을 도입부에서 짧게 인정. 사용자가 말하지 않은 새 사과·약속은 만들지 않는다.
6. 상대가 거절·원칙 통보 후 사용자가 다시 부탁하는 경우: 동일 요청에 대한 예외 가능 여부를 문의형으로. 원래 요청과 다른 주제를 발명해서 제안하는 것은 금지. 이미 "다른 방법이 없냐"고 묻고 거절된 경우에는 새로운 대안을 찾지 말고 개인 사정을 한 번 더 고려해달라는 최종 부탁 형태로 마무리한다.
7. 격식 관계에서 부탁할 때: 책임 회피 표현 대신 명확한 인정.
8. 과한 사과로 만들지 않는다.
9. draft에 요청·바람이 있었다면 rewrite에도 구체적 요청을 담는다. 서운함 표현만으로 끝내지 않는다.

---

**판단 함정 (A~F)**

- **A**: 이미 통보된 정보를 재확인 요청 금지 → 예외·대안 문의로
- **B**: 과거 합의·사전 통보 재언급 완전 금지 ("어제 ~로 하기로 했는데" 등)
- **C**: 결론 뒤집기 금지 (취소→유지, 거절→수락, 상대가 와야 함→본인이 감 등). 상대방이 와야 한다는 것이 핵심 결론이면 rewrite에도 상대에게 직접 요청하는 문장이 포함되어야 한다. 사용자 행동만 서술하고 끝내면 결론이 소멸된 것이다.
- **D**: 책임 회피 표현 금지 → 명확한 인정으로
- **E**: 충동적 포기 vs 실제 결론 판단 기준을 반드시 적용했는지 확인
- **F**: 작성 후 B·C·E를 다시 확인하고 해당하면 재작성

---

**특수 상황 패턴 (4가지)**

| 패턴 | 대응 |
|---|---|
| 즉각적 행동 요구 draft | 상대 행동 언급 없이(softened 표현도 금지) 사용자의 상황·계획만 전달 |
| 반복 패턴 우려 draft | 단정적 비난 제거, 반복될 경우 함께하기 어렵다는 우려는 반드시 살림. "아쉽다" 수준으로 희석 금지 |
| 거절 반복 압박 | 명확한 거절 유지. 질문형·협상형 변질 금지 |
| 격식 관계 + 거절 직후 | ① 본인 사정 + 책임 인정 ② 동일 요청에 대한 예외 가능 여부를 문의형으로 |

---

**GPT 출력 형식**

```json
{
  "shouldFeedback": boolean,
  "feedback": "왜 다듬는 게 좋은지 한 줄, 없으면 null",
  "rewrite": "그대로 보낼 수 있는 문장, 없으면 null",
  "reason": "어떤 감정 표현을 빼고 어떤 목적을 살렸는지 한 줄, 없으면 null"
}
```

`shouldFeedback = false`이면 나머지 필드는 모두 null로 처리.

**모델 파라미터**

| 항목 | 값 |
|---|---|
| 모델 | `gpt-4.1` (환경변수 `OPENAI_MODEL`로 변경 가능) |
| temperature | 0.3 |
| max_tokens | 500 |
| response_format | `json_object` |

---

### 5.2 유저 프롬프트 (동적 — 매 호출마다 채워짐)

```python
[대화 모드]          → "존댓말(격식체)" 또는 "반말(캐주얼)"
[나의 말투 예시]     → 최근 나의 발화 최대 3개 (어미·어휘 수준 참조용)
[최근 대화]          → 최근 6개 메시지 (나/상대방 구분)
[상대방 마지막 메시지] → 직전 상대 메시지
[사용자가 작성한 draft] → 분석 대상 초안
```

`[나의 말투 예시]`는 최근 대화에서 사용자 발화만 추출해 GPT에게 직접 전달한다. GPT가 공문서체·고객센터체가 아닌 사용자 본인의 실제 말투 스타일에 맞춰 rewrite를 생성하도록 한다.

---

## 6. 자동 응답 생성 (`/auto-reply`)

`/auto-reply` 엔드포인트는 상대방 메시지에 대한 답장 초안을 GPT로 생성한다. `reply`와 `reason` 두 필드를 반환하며, 생성 결과는 입력창에 자동 삽입된다.

자동 응답이 적용된 직후에는 피드백 타이머가 발동하지 않는다. `didUpdateWidget`에서 `isAutoReplyLoading: true → false` 전환을 감지해 이미 시작된 타이머를 즉시 취소하고, 사용자가 텍스트를 직접 수정하기 전까지의 다음 변경도 억제한다.

---

## 7. 실시간 메시지 동기화

메시지 수정·삭제 시 상대방 화면에도 실시간 반영된다.

**수정 흐름**
```
_handleEdit() → 로컬 DB editedText 갱신
             → WebSocket: {type:"EDIT", room_id, msg_timestamp, new_text}
             → 서버: 방 멤버에게 EDIT_EVENT 브로드캐스트
             → 수신측: editMessageByTimestamp() → DB 갱신 + UI 업데이트
```

**삭제 흐름**
```
_handleDelete() → 로컬 DB isDeleted=1
              → WebSocket: {type:"DELETE", room_id, msg_timestamp}
              → 서버: DELETE_EVENT 브로드캐스트
              → 수신측: deleteMessageByTimestamp() → DB 갱신 + 버블 교체
```

**메시지 식별**: 각 사용자의 SQLite DB는 독립적인 자동증가 ID를 사용하므로, `roomId + timestamp` 조합을 복합 키로 사용해 대상 메시지를 식별한다.

**이중 삽입 방지**: 채팅방이 열린 상태(`AppStyle.currentOpenRoomId == roomId`)에서는 `main_screen.dart`의 글로벌 리스너가 메시지 DB 삽입을 건너뛴다. `chat_screen.dart`의 채팅방 리스너가 단독으로 처리한다.

---

## 8. 자동 피드백 On/Off

✨ 버튼 패널(채팅 화면)과 설정 탭 두 곳에서 토글 가능하다. 설정값은 `SharedPreferences`에 저장되어 앱 재시작 후에도 유지된다. On 상태에서는 ✨ 버튼 오른쪽 상단에 초록 점이 표시된다. Off로 전환하면 진행 중인 타이머가 즉시 취소된다.

---

## 9. 테스트 케이스 (C01 ~ C25)

총 25개 시나리오를 직접 구성해 검증한다. `실습_Test/run_all_cases.py` 스크립트로 일괄 실행.

| 관계 유형 | 케이스 |
|---|---|
| 친구 (casual) | C01, C06, C11, C15, C18, C25 |
| 팀원·스터디원 (formal) | C02, C07, C10, C22 |
| 가족 - 어머니 (formal) | C03, C08 |
| 교수님·조교 (formal) | C04, C09, C19 |
| 직장 상사·팀장 (formal) | C05, C24 |
| 알바 사장님·선배 (formal) | C13, C21 |
| 판매자·고객센터 (formal) | C14, C20 |
| 후배·동아리·형제자매 (casual/formal) | C12, C16, C17, C23 |

**평가 기준**
1. 공격성 제거 — 비난·압박 표현이 제거됐는가
2. 핵심 의도 유지 — 거절·요청·결론이 뒤집히지 않았는가
3. 상대방 행동 언급 기준 준수 — ①(즉각 요구)와 ②(반복 우려)를 올바르게 구분했는가
4. 말투 일치 — `registerMode` 및 나의 실제 발화 스타일을 따르는가
5. 자연스러운 구어체 — 공문서체·고객센터체가 아닌 실제 카카오톡 말투인가
