# 성격 분석 리포트 — Leary 대인관계 모델 기술 문서

---

## 1. 개요

성격 분석 리포트는 사용자가 채팅방에서 보낸 메시지를 분석하여 **Leary 대인관계 원형 모델** 기반의 2축 수치와 9가지 캐릭터 유형을 도출한다.

분석은 백엔드의 `/analyze-leary` 엔드포인트가 담당하며, Flutter 앱의 `AnalysisScreen`이 결과를 시각화한다. 전체 채팅방 평균 또는 특정 채팅방 단위로 분석할 수 있다.

---

## 2. 분석 대상 및 데이터 수집

### 메시지 선택 기준

| 분석 모드 | 수집 방식 |
|---|---|
| 전체 평균 | 모든 채팅방에서 최근 메시지 방당 최대 30개씩 합산 |
| 단일 채팅방 | 해당 방의 최근 메시지 최대 80개 |

서버(`/analyze-leary`)에서는 수신된 메시지 중 최근 100개만 실제 분석에 사용한다.

분석 대상: 삭제되지 않고 텍스트가 비어 있지 않은 메시지. 역할은 `"me"` (사용자) 또는 `"partner"` (상대방)으로 구분된다. **수치 계산에는 사용자(`me`)의 발화만 사용하며** 상대방 메시지는 맥락 참고용이다.

---

## 3. 두 개의 핵심 축

Leary 모델은 대인관계를 **주도-순응** 축(Dominance)과 **우호-적대** 축(Affiliation)으로 분석한다.

```
         주도 (Dominance ↑)
              │
  적대 ───────┼─────── 우호
(Hostile)     │        (Friendly)
              │
         순응 (Dominance ↓)
```

두 축의 점수는 모두 **0~100** 범위로 표현된다.
- `dominance = 50`: 중립 (주도/순응 균형)
- `affiliation = 50`: 중립 (우호/적대 균형)

레이더 차트의 4개 값은 다음과 같이 파생된다:
- **주도** = `dominance`
- **우호** = `affiliation`
- **순응** = `100 - dominance`
- **적대** = `100 - affiliation`

---

## 4. 수치 계산 알고리즘

### 4.1 Affiliation (우호-적대 축) — KOTE 감정 분석 기반

사용자 발화 전체에 대해 KOTE 기반 감정 점수를 측정하고 평균을 낸다.

```
affiliation = 50.0
            + avg_warmth   × 35   (따뜻함 → 우호도 상승)
            - avg_anger    × 35   (분노 → 우호도 하락)
            - avg_burden   × 10   (부담감 → 약한 하락)
            - harsh_ratio  × 20   (공격·냉담 표현 비율 → 하락)
            - avg_distress × 5    (고통·불안 → 약한 감산)
```

`harsh_ratio`: 전체 발화 중 공격적·냉담한 표현(`_contains_harsh_expression` 또는 `_is_dismissive`)이 포함된 메시지 비율.

최종값은 **5.0 ~ 95.0** 범위로 클리핑된다.

### 4.2 Dominance (주도-순응 축) — 3i4K 화행 분류 기반

세 가지 신호를 가중 합산한다.

```
dominance = 50.0
          + speech_dominance     × 30   (화행 기반 주도성)
          + (length_ratio - 0.5) × 30   (발화 길이 비율)
          + (count_ratio  - 0.5) × 20   (발화 횟수 비율)
```

**speech_dominance**: 각 발화의 화행 유형에 아래 가중치를 적용해 평균.

| 화행 유형 | 가중치 | 의미 |
|---|---|---|
| `command`, `rhetorical_command` | 1.0 | 강한 명령·지시 |
| `request` | 0.5 | 요청 |
| `question` | 0.25 | 질문 |
| `rhetorical_question` | 0.15 | 수사적 의문 |
| `intonation_dependent` | 0.1 | 억양 의존 |
| `statement`, `fragment`, `unknown` | 0.0 | 평서·단편 |

**length_ratio**: 내 평균 발화 길이 / (내 + 상대방 평균 발화 길이). 0.5보다 크면 나의 발화가 더 길다는 의미로 주도성 상승.

**count_ratio**: 내 메시지 수 / 전체 메시지 수. 0.5보다 크면 내가 더 많이 말한다는 의미로 주도성 상승.

최종값은 **5.0 ~ 95.0** 범위로 클리핑된다.

---

## 5. 사용 NLU 모델

| 분석 대상 | 모델 | Fallback |
|---|---|---|
| 감정 점수 (warmth, anger 등) | KOTE (`source: "kote"`) | 키워드 매칭 (`source: "fallback"`) |
| 화행 분류 (command, request 등) | 3i4K (`source: "3i4k"`) | 규칙 기반 매칭 (`source: "fallback"`) |

Leary 전용 화행 분류(`_get_leary_speech_act`)는 3i4K가 `fragment`, `unknown`, `statement` 등 모호한 결과를 반환할 때 rule-based fallback으로 보정한다.

---

## 6. 신뢰도 판정

전체 발화 중 모델 기반 분석이 차지하는 비율로 신뢰도를 결정한다.

| 신뢰도 | 조건 |
|---|---|
| `high` | 감정·화행 모두 모델 분석 비율 80% 이상 |
| `medium` | 감정 또는 화행 중 하나 이상이 모델 또는 혼합 분석 |
| `low` | 대부분 fallback 또는 분석 불가 |

---

## 7. Quadrant — 9가지 캐릭터 유형

두 축의 점수를 기준으로 3×3 그리드에서 캐릭터 유형을 결정한다.

**Dominance 분류**
- `leading`: dominance ≥ 60
- `balanced`: 40 < dominance < 60
- `following`: dominance ≤ 40

**Affiliation 분류**
- `friendly`: affiliation ≥ 60
- `neutral`: 40 < affiliation < 60
- `hostile`: affiliation ≤ 40

**9가지 캐릭터 유형**

| Quadrant ID | 캐릭터명 | 아이콘 | 설명 |
|---|---|---|---|
| `leading-friendly` | 리더형 (책임적) | 책임적.png | 따뜻하게 이끄는 리더십 |
| `leading-neutral` | 추진형 (지배적) | 지배적.png | 목표 지향적, 감정 중립 |
| `leading-hostile` | 압박형 (독재적) | 독재적.png | 강한 주도권, 차가운 표현 |
| `balanced-friendly` | 협력형 (협력적) | 협력적.png | 균형 잡힌 따뜻한 소통 |
| `balanced-neutral` | 중립형 (평범적) | 평범적.png | 중립적 위치 유지 |
| `balanced-hostile` | 방어형 (의심적) | 의심적.png | 경계적, 방어적 소통 |
| `following-friendly` | 배려형 (순응적) | 순응적.png | 상대 흐름에 맞추는 배려 |
| `following-neutral` | 관망형 (의존적) | 의존적.png | 반응 위주, 감정 절제 |
| `following-hostile` | 반항형 (반항적) | 반항적.png | 소극적 저항, 방어적 표현 |

---

## 8. API 응답 구조

`POST /analyze-leary` 의 반환값:

```json
{
  "dominance": 62.5,
  "affiliation": 71.3,
  "quadrant": "leading-friendly",
  "analyzed_count": 87,
  "my_message_count": 45,
  "partner_message_count": 42,
  "window_size": 100,
  "reliability": "high",
  "sources": {
    "emotion": "kote",
    "speechAct": "3i4k"
  },
  "signals": {
    "avgWarmth": 0.312,
    "avgAnger": 0.041,
    "avgDistress": 0.085,
    "avgBurden": 0.027,
    "harshDismissiveRatio": 0.022,
    "speechDominance": 0.381,
    "lengthRatio": 0.543,
    "countRatio": 0.517
  }
}
```

---

## 9. UI 구성

`AnalysisScreen`이 분석 결과를 다음 세 영역으로 렌더링한다.

**헤더 카드**: 현재 분석 중인 채팅방(또는 전체 평균) 이름과 분석된 내 메시지 수 표시.

**Leary 카드**
- 레이더 차트: [주도, 우호, 순응, 적대] 4축 시각화
- 바 차트: 각 축 점수를 0~100 프로그레스 바로 표시
- 캐릭터 배지: 현재 quadrant에 해당하는 캐릭터명 + 아이콘 이미지

**지표 설명 카드**: 현재 성향 설명 텍스트 + 4축 의미 안내.

채팅방 선택은 앱바 우상단의 팝업 메뉴로 전환 가능하며, 한 번 분석한 결과는 `_scoreCache`에 캐시되어 동일 화면 내 재요청하지 않는다.

---

## 10. 수치 예시 해석

| dominance | affiliation | quadrant | 의미 |
|---|---|---|---|
| 75 | 78 | leading-friendly | 대화를 적극적으로 주도하면서 따뜻하고 배려 있는 소통 |
| 68 | 45 | leading-neutral | 방향을 이끌지만 감정 표현은 중립적 |
| 30 | 72 | following-friendly | 상대에게 맞추면서도 따뜻하게 반응 |
| 55 | 28 | balanced-hostile | 주도권은 균형적이나 표현이 방어적·차가운 편 |
| 50 | 50 | balanced-neutral | 완전 중립 (메시지 수 부족 또는 균형 상태) |
