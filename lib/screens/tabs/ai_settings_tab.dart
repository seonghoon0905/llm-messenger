import 'package:flutter/material.dart';

class AiSettingsTab extends StatelessWidget {
  const AiSettingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('AI 에이전트 설정 및 말투 수집 현황'));
  }
}